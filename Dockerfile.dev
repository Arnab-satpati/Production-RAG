FROM base AS dev

COPY pyproject.toml uv.lock ./
RUN uv sync --all-extras

COPY src/ src/
COPY tests/ tests/
COPY configs/ configs/

EXPOSE 8000

CMD ["uv", "run", "uvicorn", "rag.api.app:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
